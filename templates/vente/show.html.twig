{% extends 'base.html.twig' %}

{% block title %}Vente{% endblock %}

{% block body %}
    <h1>Vente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vente.id }}</td>
            </tr>
            <tr>
                <th>Attribute</th>
                <td>{{ vente.attribute }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ vente.dateDebut ? vente.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ vente.dateFin ? vente.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Heure_debut</th>
                <td>{{ vente.heureDebut ? vente.heureDebut|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Heure_fin</th>
                <td>{{ vente.heureFin ? vente.heureFin|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <br>
    <br>

    <h3>Liste des lots</h3>

    <table class="table">
        <thead>
        <tr>
            <th>Nom</th>
            <th>Prix de Depart</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for lot in lots %}
            <tr>
                <td>{{ lot.nom }}</td>
                <td>{{ lot.prixDepart }}</td>

                <td>
                    <a href="{{ path('lot_show', {'id': lot.id}) }}"><button type="button" class="btn btn-warning">enchérir</button></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <br>
    <a href="{{ path('home') }}"><button type="button" class="btn btn-primary"><i class="fas fa-arrow-left"></i> Retour</button></a>
{% endblock %}
